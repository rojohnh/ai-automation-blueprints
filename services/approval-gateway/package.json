{
  "name": "approval-gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Claude tool-calling agent with durable human-in-the-loop approval checkpoints, on Cloudflare Workers.",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "license": "MIT",
  "dependencies": {
    "@anthropic-ai/sdk": "^0.117.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260818.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
